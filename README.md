# Shapes example
This example shows you how you can draw rectangles and circles.

- The rectangle can be moved with the arrow keys. You can enlarge the rectangle using `+` and shrink it using the `-` key.
- The circle follows your mouse cursor. You can enlarge it with the left mouse button and shrink it with the right mouse button.
- When the rectangle and the circle overlap, the colors will change.